```
<script>
        // 1. 커피 : 700
        // 2. 콜라 : 600
        // 3. 녹차 : 800
        let menu = prompt('1. 커피, 2. 콜라 , 3. 녹차 메뉴를 선택하시오:');
        let input =prompt("현금을 입력하시오");
        // 유효성 검사
        // 1. 메뉴 1 ~ 3인지 확인
        // 2. input 이 숫자인가?

        // 단가를 정하기
        let unit;

        // 계산하기....
        
        if ( isNaN(Number(input)) == false ){
            let countCan = parseInt(input / unit);
            let remain = input % unit;
            console.log(`${countCan}개와 잔돈은 ${remain} 입니다.`);

        // xx가 n캔이고 잔돈은 xxx입니다.
        } else {
            console.log('숫자가 아닙니다.');
        }

        
        
    </script>
```
